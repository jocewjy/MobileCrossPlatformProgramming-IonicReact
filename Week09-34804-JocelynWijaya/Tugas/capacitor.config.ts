import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jocelynwijaya.ionic.memories',
  appName: 'Memories',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;

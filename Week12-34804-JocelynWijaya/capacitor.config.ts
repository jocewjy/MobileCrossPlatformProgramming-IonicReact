import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jocelynwijaya.week12',
  appName: 'week12',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
